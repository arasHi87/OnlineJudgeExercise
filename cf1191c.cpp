#include <cstdio>
using namespace std;
typedef long long LL;
const int maxN=1e5+10;
int n, k, m, cnt, idx, ans, qs[maxN];
int main() {
    #ifdef local
        freopen("in", "r", stdin);
    #endif
    scanf("%d%d%d", &n, &m, &k);
    for (int i=1;i<=m;i++)
        scanf("%d", qs+i);
    while (idx<=m) {
        
    }
}