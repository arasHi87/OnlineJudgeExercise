#include <cstdio>
using namespace std;
const int maxN=1e2+10;
int n, m;
int x[maxN], y[maxN], s1[maxN], p1[maxN], s2[maxN], p2[maxN];
int main() {
    #ifdef local
        freopen("in", "r", stdin);
    #endif
    while (~scanf("%d%d", &n, &m)) {
        for (int i=1;i<=n;i++)
            scanf("%d%d%d%d%d%d", x+i, y+i, s1+i, p1+i, s2+i, p2+i);
        int L=0, R=
    }
}