#include <bits/stdc++.h>
using namespace std;
const int maxN=100;

char mp[maxN][maxN];
int n, m;

int main() {
    while (~scanf("%d%d", &n, &m) && n && m) {
        string line;
        for (int i=1;i<=m;i++) {
            scanf()
        }
    }
}